1.  
    node.js 
    Visual Studio 설치
    npm install create-react-app
    npx create-react-app [프로젝트 이름]
    .env 파일 정리와 git 설치 후 연동
        gitBash 콘솔창
        git config --global user.email "[실제 아이디]"
        git config --global user.name  "[쓸 이름]"
        git config --list   

        터미널부분
        git init
        git remote add origin https://github.com/mohamohai/FootPrint.git   // 이미 진행중인 프로젝트거나 그러면 git pull로 파일 불러와서 하기
        git add .
        git status
        git commit -m "[내용]"
        git push origin master
    git pull origin master --allow-unrelated-histories

        연동 후 커밋가능
    생성 후 npm start로 실행

2.
    npm install react-router-dom 라우터 돔 관련 라이브러리 설치
